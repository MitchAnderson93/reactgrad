import type { ModalTokenMap } from '../../../../../types/modal/types';

const bootstrapModalMap: ModalTokenMap = {
  backdrop: 'modal-backdrop fade show',
  container: 'modal fade show d-block',
  header: 'modal-header',
  title: 'modal-title h5',
  closeButton: 'btn-close',
  content: 'modal-dialog modal-dialog-centered',
};

export default bootstrapModalMap;