import type { ButtonTokenMap } from '../../../../../types/button/types';

const bootstrapButtonMap: ButtonTokenMap = {
  base: 'btn', 
  variants: {
    primary: 'btn-primary',
    secondary: 'btn-secondary',
    danger: 'btn-danger', 
  },
};

export default bootstrapButtonMap;
